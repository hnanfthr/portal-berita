name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-log:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Simulasi Proses Build & Test
        run: |
          echo "Memulai proses build dan testing aplikasi..."
          echo "Proses simulasi selesai!"
